# Devices API configuration for :samples:calls-push.
#
# This committed file holds safe placeholder defaults so the project always builds.
# To use real values, copy it to `secrets.properties` (gitignored) and fill them in,
# or set the DEVICES_BASE_URL / DEVICES_API_KEY / LOAD_BALANCER_BASE_URL / SIGNALING_API_BASE_URL
# environment variables (e.g. in CI).
#
# Values are surfaced to code as BuildConfig.DEVICES_BASE_URL / BuildConfig.DEVICES_API_KEY /
# BuildConfig.LOAD_BALANCER_BASE_URL / BuildConfig.SIGNALING_API_BASE_URL.
# NOTE: BuildConfig values are embedded in the APK and are extractable
devicesBaseUrl=""
devicesApiKey=""
loadBalancerBaseUrl=""
signalingApiBaseUrl=""
signalingTokenKey=""
